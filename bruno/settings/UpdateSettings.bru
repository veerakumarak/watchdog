meta {
  name: UpdateSettings
  type: http
  seq: 2
}

put {
  url: {{host_url}}/settings
  body: json
  auth: inherit
}

body:json {
  {
          "success_retention_days": 33,
        "failure_retention_days": 90,
        "maintenance_mode": false,
        "default_channels": "eng_slack",
        "max_stage_duration_hours": 12
  
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
